'use strict';
function favIceCream(name) {
  return "I love " + name;
}

function shouting(string) {
  return string.toUpperCase();
}

function roundDown(decimal) {
  return Math.floor(decimal);
}

function theTruth(decimal) {
  return true;
}
